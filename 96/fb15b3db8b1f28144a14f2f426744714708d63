Changes V3 -> V4:

- Use common tca_flags_allowed from pkt_cls.h instead of defining it
  locally.

Changes V2 -> V3:

- Change flags type to NLA_BITFIELD32 and use nla_policy for validation.

Changes V1 -> V2:

- Defer flags validation to tcf_idr_create() function.
- Add nla_policy entry for new flags field.
