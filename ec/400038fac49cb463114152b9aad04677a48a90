Changes V4 -> V5:

- Use generic tc_action_priv_destructor typedef as a destructor type in
  tc_action_ops->get_dev().

- Wrap dev_put() in static tcf_mirred_dev_put() of type
  tc_action_priv_destructor and use it as a destructor in
  tcf_mirred_get_dev() in order to remove function type cast.

Changes V3 -> V4:

- Extend actions API get_dev() with additional destructor argument. Remove
  put_dev() from action API.

Changes V2 -> V3:

- Store direct pointer to act->ops->put_dev as a destructor function if
  entry->dev is not NULL.

Changes V1 -> V2:

- Rebase on top previous patch to use entry->destructor() and direct typed
  pointer to action ops.
