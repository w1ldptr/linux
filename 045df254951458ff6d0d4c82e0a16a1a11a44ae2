Changes V1 -> V2:

- Rename DEVLINK_SLICE_RATE_LEAF to DEVLINK_SLICE_RATE_TYPE_LEAF.

- Implement function devlink_slice_rate_notify() and
DEVLINK_CMD_SLICE_RATE_{NEW|DEL} commands and use them for notifications
when creating/deleting leaf rate object instead of putting rate object info
into notification messages for slice object themselves.
