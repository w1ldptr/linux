Changes V2 -> V3:

- Set devlink pointer in rate object before calling the driver callback
when creating new rate node and provide helper to allow driver
implementations to access the parent devlink object when creating new rate
node.
